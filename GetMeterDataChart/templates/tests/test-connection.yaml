apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "GetMeterDataChart.fullname" . }}-test-connection"
  labels:
{{ include "GetMeterDataChart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "GetMeterDataChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
